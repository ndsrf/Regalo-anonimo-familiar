-- Migration: [Brief description of what this migration does]
-- Version: [XXX - e.g., 002, 003, etc.]
-- Date: [YYYY-MM-DD]
-- Author: [Your name/email - optional]

-- INSTRUCTIONS:
-- 1. Copy this file and rename it with the next version number
--    Example: 002_add_user_preferences.sql
-- 2. Replace the placeholders above with actual information
-- 3. Write your SQL statements below
-- 4. Delete these instruction comments
-- 5. Test locally before committing

-- NOTE: Each migration is automatically wrapped in a transaction
-- Do NOT include BEGIN or COMMIT statements

-- Your SQL statements here:

-- Example: Adding a new column
-- ALTER TABLE users ADD COLUMN preferences JSONB DEFAULT '{}';

-- Example: Creating a new table
-- CREATE TABLE IF NOT EXISTS example_table (
--     id SERIAL PRIMARY KEY,
--     name VARCHAR(255) NOT NULL,
--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
-- );

-- Example: Creating an index
-- CREATE INDEX IF NOT EXISTS idx_example_name ON example_table(name);

-- Example: Adding data
-- INSERT INTO example_table (name) VALUES ('Initial value');
